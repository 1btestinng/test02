import {describe,it,expect} from 'vitest';import {rankCompanies} from './market';describe('market engine',()=>{it('calculates market cap from price and shares',()=>{const c=rankCompanies().find(x=>x.ticker==='COMI')!;expect(c.marketCapEGP).toBeCloseTo(470.49e9,0)});it('ranks largest first',()=>{const rows=rankCompanies();expect(rows[0].ticker).toBe('COMI');expect(rows.every((x,i)=>!i||rows[i-1].marketCapEGP>=x.marketCapEGP)).toBe(true)});it('converts EGP market cap to USD',()=>{const c=rankCompanies().find(x=>x.ticker==='COMI')!;expect(c.marketCapUSD).toBeCloseTo(470.49e9/51.36,0)})});
