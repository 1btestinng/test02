import Link from 'next/link';
import {getMarket} from '@/lib/markets/registry';
export default function Methodology(){return <div className="shell"><header className="header"><Link href="/" className="brand">EGYstocks</Link><Link href="/">Back to ranking</Link></header><main className="method"><div className="eyebrow">Methodology</div><h1>Simple numbers.<br/>Clear provenance.</h1><h2>Markets</h2><p>EGYstocks uses a registry-driven market architecture. Egypt is the default market and Morocco is the first expansion. Each market supplies its exchange, currency, timezone, benchmark, provider and normalized company dataset through the same interface.</p><h2>Current coverage</h2><p><b>Egypt</b> · {getMarket('EG').config.exchangeName} · EGP · delayed snapshot. <b>Morocco</b> · {getMarket('MA').config.exchangeName} · MAD · delayed snapshot. The Morocco snapshot cross-checks the official Casablanca Stock Exchange equity universe; market values come from the disclosed snapshot source. The official CSE currently lists 81 equity actions, while the active market snapshot provides market-cap data for 77 of them. The four securities without a market-cap snapshot remain listed with missing values rather than fabricated rankings.</p><h2>Ranking</h2><p>Companies are ranked independently inside the selected market from largest to smallest available market capitalization. Top 1,000 means up to 1,000 real securities available in that market; it never creates placeholder rows.</p><h2>Market cap</h2><p>Source-provided market capitalization is preferred. If a future provider only supplies price and verified shares outstanding, the normalized engine may calculate market cap as <b>share price × shares outstanding</b> and explicitly marks the result as calculated. Missing values are shown as —.</p><h2>USD conversion</h2><p>Local market capitalization is converted through the market FX layer. Egypt uses its configured EGP/USD snapshot; Morocco currently uses a disclosed USD/MAD snapshot. The FX layer is isolated so a live or licensed provider can replace the snapshot without changing the ranking UI.</p><h2>Data & delay</h2><p>Displayed data is delayed or snapshot-based and is never represented as real-time unless the configured provider says so. Every market exposes its source, timestamp, currency and delay state.</p><h2>Provider architecture</h2><p>The frontend does not call financial-data vendors directly. MarketDataProvider adapters normalize companies, quotes, market summaries, FX and market status. Adding Tunisia later should require a market configuration, a provider and a registry entry rather than a second application.</p><h2>Timezone & status</h2><p>Market status uses the selected exchange's configured timezone and market session rules. Egypt uses Africa/Cairo; Morocco uses Africa/Casablanca. The calendar logic belongs to the market/provider layer.</p></main></div>}
