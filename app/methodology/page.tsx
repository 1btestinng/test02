import Link from 'next/link';
export default function Methodology(){return <div className="shell"><header className="header"><Link href="/" className="brand">EGX<span>100</span></Link><Link href="/">Back to ranking</Link></header><main className="method"><div className="eyebrow">Methodology</div><h1>Simple numbers.<br/>Clear provenance.</h1><h2>Ranking</h2><p>Companies are ranked dynamically from largest to smallest market capitalization. Ranking positions are never hard-coded.</p><h2>Market cap</h2><p>Market capitalization is calculated as <b>share price × shares outstanding</b>. The seed snapshot reconstructs shares from the published price and market-cap pair so the engine can calculate the displayed value; these fields should be replaced by verified issuer share counts in the production database.</p><h2>USD conversion</h2><p>USD market capitalization equals EGP market capitalization divided by the stored USD/EGP rate. The current snapshot uses 51.36 EGP per USD and records the FX timestamp separately in the data layer.</p><h2>Data & delay</h2><p>Version 1 ships with a verified public snapshot sourced from Mansa Markets, whose EGX page states that its data refreshes every 30 minutes. This application does not represent that snapshot as a live EGX feed. Production market data must be supplied through the isolated MarketDataProvider adapter.</p><h2>Provider architecture</h2><p>The frontend never talks directly to a vendor. A provider adapter normalizes quotes, FX, market status and history before the ranking engine consumes them. A licensed EGX feed can therefore replace the free/delayed provider without redesigning the application.</p><h2>Timezone</h2><p>Egyptian market timestamps use Africa/Cairo. Trading-calendar logic belongs in the provider/configuration layer rather than being scattered through the UI.</p></main></div>}
